import { Outlet } from "react-router-dom";

export const OperacionesLayout = () => {
  return <Outlet />;
};

export const ControlActivosLayout = () => {
  return <Outlet />;
};

export const ProduccionLayout = () => {
  return <Outlet />;
};
